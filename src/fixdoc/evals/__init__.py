"""Layer-1 evals: the test suite for judgment.

Unit tests prove the code computes; these prove the engine JUDGES well —
against fixture data with human-approved ground truth. Development tooling,
like the pytest suite: users never run this, they receive its consequence
(defaults that were measured rather than eyeballed).
"""
